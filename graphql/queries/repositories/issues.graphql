query issueTemplates($name: String!, $owner: String!) {
    repository(name: $name, owner: $owner) {
        issueTemplates {
            name
            title
            body
            about
        }
    }
}
